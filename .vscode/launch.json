{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/bin/debug/wordcount",
      "args": [""],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Makefile",
    }
  ]
}
